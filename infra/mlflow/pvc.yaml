apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-pvc
  namespace: ml-model
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mlflow-pvc
  namespace: ml-model
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 5Gi